import React from "react";
import "../styles/About.css";

const About = () => {
  return (
    <div className="aboutTextContainer">
      <span>
        FloorApp is an app made in React.js. The main usage is to visualize the
        laying of floor panels in a room. FloorApp is still under development,
        that's why we recommend using it on desktop and we don't guarantee
        proper results.
      </span>
    </div>
  );
};

export default About;
